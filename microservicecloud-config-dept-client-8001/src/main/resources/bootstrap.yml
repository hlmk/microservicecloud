spring:
  cloud:
    config:
      name: microservicecloud-config-dept-client                # 需要从github上面读取的资源名称，注意后面没有yml后缀。
      #profile配置是什么就读取什么配置dev or test
#      profile: dev
      profile: test
      label: master
      uri: http://config-3344.com:3344                            # SpringCloudConfig获取的服务地址