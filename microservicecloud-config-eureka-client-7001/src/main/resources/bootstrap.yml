spring:
  cloud:
    config:
      name: microservicecloud-config-eureka-client                # 需要从github上面读取的资源名称，注意后面没有yml后缀。
      profile: dev
      label: master
      uri: http://config-3344.com:3344                            # SpringCloudConfig获取的服务地址